---
interface Props { s: Record<string, string>; }
const { s } = Astro.props;
const valores = [
  { icon: '🌱', title: s.home_valor1_title, text: s.home_valor1_text },
  { icon: '🔥', title: s.home_valor2_title, text: s.home_valor2_text },
  { icon: '🍯', title: s.home_valor3_title, text: s.home_valor3_text },
  { icon: '📦', title: s.home_valor4_title, text: s.home_valor4_text },
];
---
<section class="mx-auto max-w-6xl px-5 py-8">
  <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">
    {
      valores.map((v) => (
        <div class="rounded-organic border border-avena/50 bg-white/60 p-6">
          <div class="text-3xl">{v.icon}</div>
          <h3 class="mt-3 font-serif text-lg font-semibold text-tierra">{v.title}</h3>
          <p class="mt-2 text-sm leading-relaxed text-tierra/70">{v.text}</p>
        </div>
      ))
    }
  </div>
</section>
